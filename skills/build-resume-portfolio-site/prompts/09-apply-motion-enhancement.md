---
resource_id: apply-motion-enhancement
resource_version: 1
resource_status: ready
output_contract: react-vite-project-update-and-motion-enhancement-plan-json
---

# Apply compatible selected motion recipes to the confirmed Vite site

Restore the immutable `versions/v4-motion` snapshot before editing. Apply all compatible selected primary recipes and secondary effects; there is no numeric effect cap. Honor every target/controller conflict_resolution by sharing the documented timeline or isolating the affected section. Treat the confirmed Poster as a valid complete media asset; do not block completion on video.

Change only the approved motion/media layer. Preserve resume facts, copy, section order, palette, typography, component hierarchy, breakpoint behavior, and the confirmed media direction baseline. Use a `data-motion-media` component with a fixed aspect ratio and Poster `<img>` fallback. Do not introduce scroll-linked video, `currentTime` scrubbing, pointer-linked playback, remote video URLs, or autoplay audio.

Write `reports/motion-enhancement-plan.json`, validate the `motion-enhanced` stage, run the production build, capture desktop/mobile screenshots, and snapshot `versions/v5-motion-enhanced-poster` only after confirmation.
